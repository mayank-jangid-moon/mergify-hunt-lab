queue_rules:
  - name: default
    conditions:
      - base=main
    commit_message_template: |
      PRCFG-MARKER {{ title }} (#{{ number }})

pull_request_rules:
  - name: automerge
    conditions:
      - base=main
    actions:
      queue:
        name: default
