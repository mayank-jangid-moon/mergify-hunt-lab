queue_rules:
  - name: default
    conditions:
      - base=main
    commit_message_template: |
      {{ title }} probe-autofix-{{ 7*7 }}

pull_request_rules:
  - name: automerge
    conditions:
      - base=main
    actions:
      queue:
        name: nonexistent_queue_xyz
