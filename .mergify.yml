queue_rules:
  - name: default
    conditions:
      - base=main

pull_request_rules:
  - name: automerge
    conditions:
      - base=main
    actions:
      queue:
        name: default
      invalid_action_xyz:
        foo: bar

bogus_top_level_key: true
