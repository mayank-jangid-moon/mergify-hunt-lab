queue_rules:
  - name: default
    conditions:
      - base=main
    commit_message_template: |
      {{ title }} (#{{ number }})
      RT: {{ title|attr("format")("{0.__class__}") }}

pull_request_rules:
  - name: automerge
    conditions:
      - base=main
    actions:
      queue:
        name: default
